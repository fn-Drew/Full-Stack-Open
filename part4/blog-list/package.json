{
    "scripts": {
        "start": "NODE_ENV=production node index.js",
        "dev": "NODE_ENV=development nodemon app.js",
        "test": "NODE_ENV=test jest --verbose",
        "testleaks": "NODE_ENV=test jest --verbose --runInBand --detectOpenHandles"
    },
    "devDependencies": {
        "jest": "^29.4.3",
        "nodemon": "^2.0.20",
        "supertest": "^6.3.3"
    },
    "dependencies": {
        "cors": "^2.8.5",
        "dotenv": "^16.0.3",
        "express": "^4.18.2",
        "mongoose": "^6.9.2"
    },
    "jest": {
        "testEnvironment": "node"
    }
}
