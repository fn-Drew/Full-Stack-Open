{
    "scripts": {
        "dev": "nodemon app.js",
        "test": "jest --verbose",
        "testleaks": "jest --verbose --runInBand --detectOpenHandles"
    },
    "devDependencies": {
        "jest": "^29.4.3",
        "nodemon": "^2.0.20",
        "supertest": "^6.3.3"
    },
    "dependencies": {
        "cors": "^2.8.5",
        "dotenv": "^16.0.3",
        "express": "^4.18.2",
        "mongoose": "^6.9.2"
    },
    "jest": {
        "testEnvironment": "node"
    }
}
